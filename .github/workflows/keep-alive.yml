name: Keep Alive
on:
  schedule:
    - cron: '0 * * * *'  # 매 시간마다 실행
jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl https://marketingkeywordrecommend.onrender.com/health